apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: envapp
spec:
  template:
    spec:
      containers:
      - image: github.com/savitaashture/simple-go-app/helloworld
        env:
        - name: TARGET
          value: "v1"
